import { Canvas } from 'canvas';
import { ComputedOptions } from '../@types';
export interface BubbleTailOptions {
    width: number;
    height: number;
}
declare const _default: ({ width, height }: BubbleTailOptions) => (canvas: Canvas, conf: ComputedOptions) => Canvas;
export default _default;
